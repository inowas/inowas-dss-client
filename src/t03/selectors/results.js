export const getCalculationID = state => state ? state.calculation_id : null;
export const getLayerValues = state => state ? state.layer_values : null;
export const getTimes = state => state ? state.times : null;
